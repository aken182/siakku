@if (isset($coa))
    @php
        $coa = $coa;
    @endphp
@else
    @php
        $coa = '';
    @endphp
@endif

@if (isset($barang))
    @php
        $barang = $barang;
    @endphp
@else
    @php
        $barang = '';
    @endphp
@endif

@if (isset($unit))
    @php
        $unit = $unit;
    @endphp
@else
    @php
        $unit = '';
    @endphp
@endif

@if (isset($satuan))
    @php
        $satuan = $satuan;
    @endphp
@else
    @php
        $satuan = '';
    @endphp
@endif

@if (isset($detail))
    @php
        $detail = $detail;
    @endphp
@else
    @php
        $detail = '';
    @endphp
@endif

@if (isset($debet))
    @php
        $debet = $debet;
    @endphp
@else
    @php
        $debet = '';
    @endphp
@endif

@if (isset($kredit))
    @php
        $kredit = $kredit;
    @endphp
@else
    @php
        $kredit = '';
    @endphp
@endif
